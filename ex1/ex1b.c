#include <stdio.h>
#include <stdlib.h>

int main() {
    
    int a, b, c;
    
    printf("Integer a is stored in address: %#010x\n", &a);
    printf("Integer b is stored in address: %#010x\n", &b);
    printf("Integer c is stored in address: %#010x\n", &c);

    return 0;
    
}

